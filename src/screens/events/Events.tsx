import React from 'react';
import {Text} from 'react-native';
import {Colors} from 'react-native/Libraries/NewAppScreen';

export const Events = () => {
  return (
    <Text style={{color: 'red', textAlign: 'center', fontSize: 20}}>
      Sorties moto en construction
    </Text>
  );
};
